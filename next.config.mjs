/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "images.unsplash.com"], // <-- add your external domains here
  },
};

export default nextConfig;
